# Energy-demand practice — long multivariate hourly load; Advanced EDA + Time Series (Python 3.12).
pandas>=2.2,<3
numpy>=1.26,<2.2
scipy>=1.13
matplotlib>=3.8
seaborn>=0.13
statsmodels>=0.14       # STL/MSTL decomposition, ACF/PACF, stationarity tests
scikit-learn>=1.4       # PCA, ML forecasting, anomaly detection
statsforecast>=1.7      # AutoARIMA / fast classical forecasting (NumPy-2 safe, unlike pmdarima)
lightgbm>=4.5           # gradient-boosted load forecasting
holidays>=0.50          # US federal holiday calendar (demand drops on holidays)

# Notebook tooling
jupyter
ipykernel
nbconvert
ruptures>=1.1            # PELT change-point detection (Part 6)
