# Core scientific + EDA + time-series stack.
# Pinned to a known-good Python 3.12 wheel set.
pandas>=2.2,<3
numpy>=1.26,<2.2
scipy>=1.13
matplotlib>=3.8
seaborn>=0.13
statsmodels>=0.14
scikit-learn>=1.4

# Notebook tooling
jupyter
ipykernel
nbconvert

# Forecasting
statsforecast>=2.0     # AutoARIMA (Part 3) — the NumPy-2-safe replacement for pmdarima
lightgbm>=4.5          # gradient-boosted trees (Part 5)
prophet>=1.1           # additive decomposition model (Part 5)
# NOTE: pmdarima is intentionally NOT used — it is incompatible with NumPy 2.x.
